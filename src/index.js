import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';  // Add CSS import for styling if needed
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);